# Aurora CloudBank Symbolic - Test and CI Dependencies
# Install with core requirements:
#   pip install -r requirements.txt -r requirements-test.txt

# Test execution and coverage
pytest>=9.1.1
pytest-asyncio>=0.23.0
pytest-cov>=7.0.0
coverage>=7.15.2

# Syntax gate used by CI
flake8>=6.1.0

# Connector test support
# Runtime use remains optional, but tests/connector and test_mcp_connector_wired
# import the MCP SDK during collection.
mcp>=1.28.1
