# Copy the values you want into a local .env.mesh file at the repo root.
# The launcher and live adapter will read .env.mesh automatically.

OPENAI_API_KEY=
MESH_OPENAI_MODEL=gpt-4.1-mini
MESH_ROUTER_URL=http://127.0.0.1:8000
