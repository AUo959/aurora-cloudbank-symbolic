# Dependabot configuration
#
# Nine ecosystem/directory entries previously ran ungrouped at a limit of five
# each — up to 45 concurrent pull requests, one per dependency. In practice that
# produced a standing backlog of ~23, which reads to any visitor as an
# unmaintained repository regardless of how current the code actually is.
#
# Every entry below groups its updates, so a week's worth of bumps arrives as
# one reviewable PR per ecosystem rather than one per package. Limits are low
# because grouping makes them sufficient: if a limit is ever hit, that is a
# signal the queue is not being reviewed, not a reason to raise it.
#
# Majors are deliberately separated from patch/minor on the larger ecosystems.
# A grouped patch PR is usually safe to merge on green CI; a major needs
# reading. Mixing them forces the whole group to move at the speed of its
# riskiest member — which is how the previous backlog accumulated.

version: 2
updates:
  # ── CI infrastructure ──────────────────────────────────────────────────
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "github-actions"]
    groups:
      actions:
        patterns: ["*"]

  - package-ecosystem: "pre-commit"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:15"
      timezone: "America/New_York"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "pre-commit"]
    groups:
      pre-commit:
        patterns: ["*"]

  # ── Python ─────────────────────────────────────────────────────────────
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:30"
      timezone: "America/New_York"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "python"]
    # pydantic-core is not an independently upgradable package: each pydantic
    # release pins an EXACT pydantic-core build (pydantic 2.13.4 requires
    # pydantic-core==2.46.4). Dependabot was proposing pydantic-core on its own,
    # which is unsatisfiable -- no pydantic release pairs with it yet -- so the
    # PR failed with ResolutionImpossible and simply re-rolled each week:
    # #1390, then #1412, identical failure both times.
    #
    # Ignoring it here does not freeze it. It still moves whenever pydantic
    # moves, because regenerating the lock resolves it transitively; it just
    # stops being offered as a standalone bump.
    #
    # mcp majors are held back because the connector is written against the
    # MCP v1 low-level API: connector/server.py registers handlers with
    # @server.list_tools() / @server.call_tool(), which mcp 2.x removed. A
    # v2 bump therefore fails the connector suite with
    # "AttributeError: 'Server' object has no attribute 'list_tools'" -- the
    # baseline defect tracked in #1384, which #1421 would have re-landed.
    #
    # This pins the ceiling in one place rather than relying on every future
    # grouped bump being caught in review. Minor and patch releases within v1
    # still flow normally. Lift this together with the connector migration to
    # the v2 handler API, not before.
    ignore:
      - dependency-name: "pydantic-core"
      - dependency-name: "mcp"
        update-types: ["version-update:semver-major"]
    groups:
      python-patch-minor:
        patterns: ["*"]
        update-types: ["minor", "patch"]
      python-major:
        patterns: ["*"]
        update-types: ["major"]

  - package-ecosystem: "pip"
    directory: "/cli"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:45"
      timezone: "America/New_York"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "python"]
    groups:
      cli-python:
        patterns: ["*"]

  - package-ecosystem: "pip"
    directory: "/sdk/python"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "python"]
    groups:
      sdk-python:
        patterns: ["*"]

  - package-ecosystem: "pip"
    directory: "/services/nemo_service"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:15"
      timezone: "America/New_York"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "python", "service"]
    groups:
      nemo-service:
        patterns: ["*"]

  # ── JavaScript ─────────────────────────────────────────────────────────
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:30"
      timezone: "America/New_York"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "deps"
    labels: ["dependencies"]
    groups:
      npm-patch-minor:
        patterns: ["*"]
        update-types: ["minor", "patch"]
      npm-major:
        patterns: ["*"]
        update-types: ["major"]

  - package-ecosystem: "npm"
    directory: "/frontend"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:45"
      timezone: "America/New_York"
    open-pull-requests-limit: 3
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "frontend"]
    # typescript majors are held back because the linter cannot follow yet.
    # @typescript-eslint/eslint-plugin declares
    # "peer typescript >=4.8.4 <6.1.0", and that range is unchanged as of its
    # latest release (8.66.0) -- there is no published version that accepts
    # TypeScript 7. So a typescript@^7 bump does not merely risk type errors,
    # it makes `npm install` fail outright with ERESOLVE, which took the whole
    # frontend-major group down with it in #1443: React 19, R3F 9, zod 4 and
    # 20 other majors could not even be evaluated because the tree would not
    # install.
    #
    # Holding typescript here lets the rest of the group be reviewed on a
    # build that actually runs. Minor and patch releases within the current
    # major still flow normally. Lift this once typescript-eslint ships a
    # release whose peer range admits TypeScript 7, and bump the two
    # @typescript-eslint/* packages in the same PR.
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
    groups:
      frontend-patch-minor:
        patterns: ["*"]
        update-types: ["minor", "patch"]
      frontend-major:
        patterns: ["*"]
        update-types: ["major"]

  - package-ecosystem: "npm"
    directory: "/services/command_node"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "10:00"
      timezone: "America/New_York"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "deps"
    labels: ["dependencies", "service"]
    groups:
      command-node:
        patterns: ["*"]
