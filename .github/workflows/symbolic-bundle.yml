name: Disable symbolic-bundle workflow  

on:  
  push:  
    branches:  
      - main  
  pull_request:  
    branches:  
      - main  

jobs:  
  build:  
    runs-on: ubuntu-latest  
    steps:  
      - name: Checkout  
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3

      - name: Run Build  
        run: echo 'Building...'  

      - name: Disable  
        if: false  
        run: echo 'This step will never run'  
